function possible_conditions(cond)
    # Return all possible conditions that a row corresponds to
    # by replacing ? with either . or #

    # First find all indices of ?
    ind = findall('?', cond)

    # For each possible combination
        # Generate a vector of some #s and some #.
            # 
        
        # Substitute

    # Return
end
